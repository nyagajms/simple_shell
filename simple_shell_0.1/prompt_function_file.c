#include"mjsh_main.h"

/**
 * prompt - function that dispalys the prompt which include
 * information about the current user and directory
 * the prompt function is called to provide a visual
 * cue to the user that the shell is ready to accept commands
*/

void prompt(void)
{
	_printf("┌──(moreen@james)\n└─$");
}
