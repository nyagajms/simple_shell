#include"mjsh_main.h"
/**
 * main - main shell file
 * Return: 0
*/

int main (void)
{
    void prompt(void);
    void instraction_reader(char *text, size_t length);
    void execution(const char *text);
    return (0);
}